# 🎬 Кастомный видео-плеер для блога

## 📋 Что реализовано

✅ **Кастомный видео-плеер** с полным контролем  
✅ **Загрузка медиа** (изображения и видео)  
✅ **Галерея медиа** с предпросмотром  
✅ **Интеграция в Markdown** - автоматический рендеринг  
✅ **Supabase Storage** - хранение файлов  
✅ **Адаптивный дизайн** - работает на всех устройствах  

---

## 🎨 Компоненты

### 1. CustomVideoPlayer.jsx
Полнофункциональный видео-плеер с:
- Play/Pause кнопкой
- Регулировкой громкости
- Прогресс-баром с перемоткой
- Полноэкранным режимом
- Автоскрытием контролов
- Красивым дизайном

### 2. MediaUploader.jsx
Компонент для загрузки файлов:
- Поддержка изображений и видео
- Прогресс загрузки
- Валидация файлов
- Обработка ошибок

### 3. MediaGallery.jsx
Галерея загруженных медиа:
- Вкладки для изображений и видео
- Предпросмотр файлов
- Копирование URL
- Удаление файлов
- Выбор для вставки в посты

---

## 🚀 Установка

### Шаг 1: Настройка Supabase Storage

1. Откройте Supabase Dashboard
2. Перейдите в **Storage**
3. Создайте три bucket:
   - `images` (Public: ON)
   - `videos` (Public: ON)
   - `avatars` (Public: ON)

### Шаг 2: Настройка политик доступа

Выполните SQL скрипт `fix-storage-policies.sql` в Supabase SQL Editor:

```bash
# В Supabase Dashboard
# SQL Editor → New query
# Вставьте содержимое fix-storage-policies.sql
# Нажмите RUN
```

### Шаг 3: Готово! ✅

Все компоненты уже созданы и готовы к использованию.

---

## 📖 Использование

### В постах (Markdown)

#### Вставка видео:
```markdown
[🎥 Video: Название видео](https://your-url.com/video.mp4)
```

#### Вставка изображения:
```markdown
![Описание](https://your-url.com/image.jpg)
```

### В коде (React)

#### Кастомный плеер:
```jsx
import CustomVideoPlayer from './components/CustomVideoPlayer'

<CustomVideoPlayer 
  src="https://example.com/video.mp4"
  title="Название видео"
  poster="https://example.com/poster.jpg"
/>
```

#### Загрузка файлов:
```jsx
import MediaUploader from './components/MediaUploader'

<MediaUploader 
  type="video"  // 'image', 'video', или 'both'
  onMediaUploaded={(result) => {
    console.log('URL:', result.url)
    console.log('Размер:', result.size)
  }}
/>
```

#### Галерея:
```jsx
import MediaGallery from './components/MediaGallery'

<MediaGallery 
  onSelectMedia={(item) => {
    console.log('Выбрано:', item.url)
  }}
/>
```

---

## 🔧 API

### SupabaseService методы:

```javascript
import supabaseService from './services/SupabaseService'

// Загрузка изображения
const imageResult = await supabaseService.uploadImage(file, 'blog-images')
// Возвращает: { url, path, fileName, size, type }

// Загрузка видео
const videoResult = await supabaseService.uploadVideo(file, 'blog-videos')
// Возвращает: { url, path, fileName, size, type, name }

// Удаление изображения
await supabaseService.deleteImage(filePath)

// Удаление видео
await supabaseService.deleteVideo(filePath)

// Получение списка изображений
const images = await supabaseService.getImages('blog-images')

// Получение списка видео
const videos = await supabaseService.getVideos('blog-videos')
```

---

## 📊 Технические детали

### Ограничения размеров:
- **Изображения**: 5 MB
- **Видео**: 50 MB
- **Аватарки**: 2 MB

### Поддерживаемые форматы:

**Изображения:**
- JPG, JPEG
- PNG
- GIF
- WebP
- SVG

**Видео:**
- MP4 (рекомендуется, H.264 кодек)
- WebM
- AVI
- MOV
- MKV
- M4V

### Структура Storage:

```
supabase-storage/
├── images/
│   ├── blog-images/
│   │   └── [uploaded-images]
│   └── avatars/
│       └── [user-avatars]
└── videos/
    └── blog-videos/
        └── [uploaded-videos]
```

---

## 🎯 Возможности плеера

### Основные:
- ▶️ **Play/Pause** - воспроизведение и пауза
- 🔊 **Volume Control** - регулировка громкости
- 🔇 **Mute/Unmute** - отключение звука
- ⏱️ **Seek Bar** - перемотка видео
- 🖥️ **Fullscreen** - полноэкранный режим
- ⏲️ **Time Display** - отображение времени

### Дополнительные:
- 🎨 **Адаптивный дизайн** - работает на всех устройствах
- 🎯 **Автоскрытие контролов** - контролы скрываются при воспроизведении
- 🌈 **Красивый UI** - современный дизайн с градиентами
- ⚡ **Плавные анимации** - transitions и hover эффекты
- 📱 **Touch-friendly** - удобно на мобильных устройствах

---

## 🔄 Интеграция в админ-панель

Добавьте в `Admin.jsx`:

```jsx
import MediaUploader from '../components/MediaUploader'
import MediaGallery from '../components/MediaGallery'

// В редакторе постов:
<div className="flex gap-2 mb-4">
  <MediaUploader 
    type="image"
    onMediaUploaded={(result) => {
      const markdown = `\n![Изображение](${result.url})\n`
      setPostData(prev => ({
        ...prev,
        content: prev.content + markdown
      }))
    }}
  />
  
  <MediaUploader 
    type="video"
    onMediaUploaded={(result) => {
      const markdown = `\n[🎥 Video: ${result.fileName}](${result.url})\n`
      setPostData(prev => ({
        ...prev,
        content: prev.content + markdown
      }))
    }}
  />
</div>
```

Подробнее см. `ADMIN-MEDIA-INTEGRATION.md`

---

## 🐛 Решение проблем

### Видео не загружается

**Проблема:** Ошибка при загрузке видео

**Решение:**
1. Проверьте, что bucket `videos` создан
2. Убедитесь, что bucket публичный (Public: ON)
3. Выполните `fix-storage-policies.sql`
4. Проверьте размер файла (макс. 50 MB)
5. Проверьте формат (MP4 рекомендуется)

### Видео не воспроизводится

**Проблема:** Видео загружено, но не воспроизводится

**Решение:**
1. Проверьте URL (должен быть публичным)
2. Используйте MP4 с H.264 кодеком
3. Откройте консоль браузера для ошибок
4. Проверьте CORS настройки в Supabase

### Ошибка прав доступа

**Проблема:** "Permission denied" при загрузке

**Решение:**
```sql
-- Выполните в Supabase SQL Editor
CREATE POLICY "Anyone can upload videos"
ON storage.objects FOR INSERT
WITH CHECK (bucket_id = 'videos');
```

### Плеер не отображается

**Проблема:** Видео есть, но плеер не рендерится

**Решение:**
1. Проверьте импорт `CustomVideoPlayer` в `markdownRenderer.jsx`
2. Проверьте синтаксис: `[🎥 Video: Название](URL)`
3. Откройте консоль для ошибок React

---

## 📁 Структура файлов

```
my-blog-main/
├── src/
│   ├── components/
│   │   ├── CustomVideoPlayer.jsx      # Кастомный плеер
│   │   ├── MediaUploader.jsx          # Загрузчик файлов
│   │   └── MediaGallery.jsx           # Галерея медиа
│   ├── services/
│   │   └── SupabaseService.js         # API методы (уже обновлен)
│   └── utils/
│       └── markdownRenderer.jsx       # Рендеринг (обновлен)
├── fix-storage-policies.sql           # SQL для настройки
├── MEDIA-UPLOAD-GUIDE.md             # Полное руководство
├── ADMIN-MEDIA-INTEGRATION.md        # Интеграция в админку
├── MEDIA-CHEATSHEET.md               # Быстрая шпаргалка
├── test-video-post.md                # Тестовый пост
└── README-CUSTOM-VIDEO-PLAYER.md     # Этот файл
```

---

## 🎓 Примеры

### Пример 1: Простая загрузка

```jsx
const handleUpload = async (file) => {
  try {
    const result = await supabaseService.uploadVideo(file)
    alert('Загружено: ' + result.url)
  } catch (error) {
    alert('Ошибка: ' + error.message)
  }
}
```

### Пример 2: С прогрессом

```jsx
const [uploading, setUploading] = useState(false)

const handleUpload = async (file) => {
  setUploading(true)
  try {
    const result = await supabaseService.uploadVideo(file)
    console.log('URL:', result.url)
  } finally {
    setUploading(false)
  }
}
```

### Пример 3: Галерея с выбором

```jsx
const [selectedVideo, setSelectedVideo] = useState(null)

<MediaGallery 
  onSelectMedia={(item) => {
    setSelectedVideo(item)
    // Вставляем в редактор
    insertVideoIntoPost(item.url)
  }}
/>
```

---

## 🌟 Фичи

### Реализовано:
- ✅ Кастомный видео-плеер
- ✅ Загрузка изображений и видео
- ✅ Галерея медиа
- ✅ Интеграция в Markdown
- ✅ Supabase Storage
- ✅ Адаптивный дизайн
- ✅ Обработка ошибок
- ✅ Валидация файлов

### Можно добавить:
- 🔄 Drag & Drop загрузка
- 📊 Прогресс загрузки с процентами
- 🎨 Редактор изображений
- 📹 Генерация превью для видео
- 🔍 Поиск по медиа
- 📁 Папки и категории
- 🏷️ Теги для медиа
- 📈 Статистика использования

---

## 📚 Дополнительные ресурсы

- [Supabase Storage Documentation](https://supabase.com/docs/guides/storage)
- [HTML5 Video API](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/video)
- [React Best Practices](https://react.dev/learn)

---

## 🤝 Поддержка

Если возникли проблемы:
1. Проверьте консоль браузера
2. Проверьте Supabase Dashboard → Storage
3. Проверьте политики доступа
4. Откройте `MEDIA-CHEATSHEET.md` для быстрых решений

---

## ✅ Чеклист запуска

- [ ] Создал buckets в Supabase (images, videos, avatars)
- [ ] Установил buckets как Public
- [ ] Выполнил `fix-storage-policies.sql`
- [ ] Проверил, что компоненты импортированы
- [ ] Загрузил тестовое видео
- [ ] Проверил воспроизведение в посте
- [ ] Интегрировал в админ-панель (опционально)

---

## 🎉 Готово!

Теперь у вас есть полнофункциональная система загрузки и воспроизведения медиа с кастомным видео-плеером!

**Попробуйте:**
1. Загрузите видео через `MediaUploader`
2. Вставьте в пост: `[🎥 Video: Название](URL)`
3. Наслаждайтесь кастомным плеером! 🚀

---

**Версия:** 1.0.0  
**Дата:** 2026-01-18  
**Автор:** Kiro AI Assistant
