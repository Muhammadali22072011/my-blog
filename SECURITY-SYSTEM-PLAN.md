# 🔐 План системы безопасности и управления пользователями

## 📋 Текущее состояние

✅ **Что уже есть:**
- Логирование просмотров страниц
- Определение IP адресов и геолокации
- Отслеживание браузеров и устройств
- Админ панель с логами безопасности

❌ **Чего нет:**
- Системы регистрации/логина пользователей
- Управления пользователями
- Блокировки по IP
- Защиты от спама и DDoS

---

## 🎯 Полный план системы безопасности

### 1️⃣ Система регистрации и авторизации

#### Что нужно:
```
📝 Регистрация:
- Email + пароль
- Подтверждение email
- Проверка на спам (капча)

🔐 Вход:
- Email + пароль
- "Запомнить меня"
- Восстановление пароля

👤 Профиль пользователя:
- Имя, фото, био
- История активности
- Настройки приватности
```

#### Таблицы в базе данных:
```sql
-- Supabase уже имеет таблицу auth.users
-- Нужно добавить таблицу profiles:

CREATE TABLE profiles (
  id UUID REFERENCES auth.users(id) PRIMARY KEY,
  username TEXT UNIQUE,
  full_name TEXT,
  avatar_url TEXT,
  bio TEXT,
  role TEXT DEFAULT 'user', -- 'user', 'moderator', 'admin'
  is_blocked BOOLEAN DEFAULT false,
  blocked_reason TEXT,
  blocked_until TIMESTAMPTZ,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);
```

#### Компоненты:
- `Login.jsx` - форма входа
- `Register.jsx` - форма регистрации
- `ForgotPassword.jsx` - восстановление пароля
- `Profile.jsx` - профиль пользователя
- `AuthContext.jsx` - контекст авторизации

---

### 2️⃣ Управление пользователями (Admin Panel)

#### Функции:
```
👥 Список пользователей:
- Все зарегистрированные пользователи
- Фильтры: роль, статус, дата регистрации
- Поиск по email/имени

🔧 Действия с пользователями:
- Просмотр профиля и активности
- Изменение роли (user → moderator → admin)
- Блокировка/разблокировка
- Удаление пользователя
- Отправка email уведомления

📊 Статистика:
- Всего пользователей
- Новых за неделю/месяц
- Активных пользователей
- Заблокированных
```

#### Таблица в базе:
```sql
CREATE TABLE user_actions (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  user_id UUID REFERENCES auth.users(id),
  action_type TEXT, -- 'comment', 'like', 'bookmark', 'report'
  target_type TEXT, -- 'post', 'comment', 'user'
  target_id UUID,
  created_at TIMESTAMPTZ DEFAULT NOW()
);
```

#### Компонент:
- `UserManagement.jsx` - управление пользователями в админке

---

### 3️⃣ Блокировка по IP адресам

#### Функции:
```
🚫 Черный список IP:
- Ручная блокировка IP
- Автоматическая блокировка при подозрительной активности
- Временная блокировка (1 час, 1 день, навсегда)
- Причина блокировки

✅ Белый список IP:
- Доверенные IP (никогда не блокируются)
- Например: твой домашний IP, офис

📊 Статистика:
- Заблокированные IP
- Попытки доступа с заблокированных IP
- История блокировок
```

#### Таблица в базе:
```sql
CREATE TABLE ip_blacklist (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  ip_address TEXT UNIQUE NOT NULL,
  reason TEXT,
  blocked_by UUID REFERENCES auth.users(id),
  blocked_until TIMESTAMPTZ, -- NULL = навсегда
  is_permanent BOOLEAN DEFAULT false,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE TABLE ip_whitelist (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  ip_address TEXT UNIQUE NOT NULL,
  description TEXT,
  added_by UUID REFERENCES auth.users(id),
  created_at TIMESTAMPTZ DEFAULT NOW()
);
```

#### Компонент:
- `IPBlacklist.jsx` - управление черным списком IP

#### Middleware:
```javascript
// Проверка IP при каждом запросе
const checkIPBlacklist = async (ip) => {
  const { data } = await supabase
    .from('ip_blacklist')
    .select('*')
    .eq('ip_address', ip)
    .single()
  
  if (data) {
    if (data.is_permanent) {
      return { blocked: true, reason: data.reason }
    }
    if (data.blocked_until && new Date(data.blocked_until) > new Date()) {
      return { blocked: true, reason: data.reason }
    }
  }
  
  return { blocked: false }
}
```

---

### 4️⃣ Защита от спама и DDoS

#### Функции:
```
⚡ Rate Limiting:
- Максимум 100 запросов в минуту с одного IP
- Максимум 10 комментариев в час
- Максимум 5 попыток входа в 15 минут

🤖 Защита от ботов:
- Google reCAPTCHA на формах
- Проверка User-Agent
- Honeypot поля (скрытые поля для ботов)

📧 Защита от спама в комментариях:
- Фильтр запрещенных слов
- Проверка на дубликаты
- Модерация новых пользователей
```

#### Таблица в базе:
```sql
CREATE TABLE rate_limits (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  ip_address TEXT NOT NULL,
  action_type TEXT NOT NULL, -- 'request', 'comment', 'login'
  count INTEGER DEFAULT 1,
  window_start TIMESTAMPTZ DEFAULT NOW(),
  created_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE TABLE spam_filters (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  filter_type TEXT, -- 'word', 'pattern', 'user_agent'
  filter_value TEXT,
  is_active BOOLEAN DEFAULT true,
  created_at TIMESTAMPTZ DEFAULT NOW()
);
```

#### Middleware:
```javascript
// Rate limiting
const checkRateLimit = async (ip, actionType, limit, windowMinutes) => {
  const windowStart = new Date(Date.now() - windowMinutes * 60 * 1000)
  
  const { data } = await supabase
    .from('rate_limits')
    .select('count')
    .eq('ip_address', ip)
    .eq('action_type', actionType)
    .gte('window_start', windowStart.toISOString())
    .single()
  
  if (data && data.count >= limit) {
    return { limited: true, message: 'Too many requests' }
  }
  
  return { limited: false }
}
```

---

### 5️⃣ Мониторинг подозрительной активности

#### Что отслеживать:
```
⚠️ Подозрительные действия:
- Слишком много запросов (DDoS)
- Много неудачных попыток входа (брутфорс)
- Спам в комментариях
- Подозрительный User-Agent (боты)
- Доступ к несуществующим страницам (сканирование)

🔔 Автоматические действия:
- Временная блокировка IP (1 час)
- Email уведомление админу
- Логирование в security_audit_log
```

#### Таблица в базе:
```sql
CREATE TABLE security_alerts (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  alert_type TEXT, -- 'ddos', 'bruteforce', 'spam', 'bot'
  severity TEXT, -- 'low', 'medium', 'high', 'critical'
  ip_address TEXT,
  user_id UUID REFERENCES auth.users(id),
  description TEXT,
  is_resolved BOOLEAN DEFAULT false,
  resolved_by UUID REFERENCES auth.users(id),
  resolved_at TIMESTAMPTZ,
  created_at TIMESTAMPTZ DEFAULT NOW()
);
```

#### Компонент:
- `SecurityAlerts.jsx` - уведомления о подозрительной активности

---

### 6️⃣ Email уведомления

#### Когда отправлять:
```
📧 Пользователю:
- Вход с нового устройства/IP
- Изменение пароля
- Блокировка аккаунта
- Подозрительная активность

📧 Админу:
- Новая регистрация
- Подозрительная активность
- Блокировка IP
- Критические ошибки
```

#### Таблица в базе:
```sql
CREATE TABLE email_notifications (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  user_id UUID REFERENCES auth.users(id),
  email TEXT NOT NULL,
  subject TEXT,
  body TEXT,
  notification_type TEXT,
  is_sent BOOLEAN DEFAULT false,
  sent_at TIMESTAMPTZ,
  error TEXT,
  created_at TIMESTAMPTZ DEFAULT NOW()
);
```

---

### 7️⃣ Двухфакторная аутентификация (2FA)

#### Как работает:
```
1. Пользователь включает 2FA в настройках
2. При входе:
   - Вводит email + пароль
   - Получает код на email
   - Вводит код
   - Вход разрешен

Альтернатива:
- TOTP (Google Authenticator)
- SMS код (платно)
```

#### Таблица в базе:
```sql
CREATE TABLE two_factor_auth (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  user_id UUID REFERENCES auth.users(id) UNIQUE,
  is_enabled BOOLEAN DEFAULT false,
  method TEXT, -- 'email', 'totp', 'sms'
  secret TEXT, -- для TOTP
  backup_codes TEXT[], -- резервные коды
  created_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE TABLE two_factor_codes (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  user_id UUID REFERENCES auth.users(id),
  code TEXT NOT NULL,
  expires_at TIMESTAMPTZ,
  is_used BOOLEAN DEFAULT false,
  created_at TIMESTAMPTZ DEFAULT NOW()
);
```

---

### 8️⃣ Аналитика безопасности

#### Дашборд:
```
📊 Графики:
- Активность по часам/дням
- География посетителей (карта)
- Топ браузеров и устройств
- Заблокированные IP

📈 Метрики:
- Всего посетителей
- Уникальных IP
- Заблокированных попыток
- Подозрительной активности

🗺️ Карта:
- Интерактивная карта с точками посетителей
- Клик на точку → детали
```

#### Компонент:
- `SecurityDashboard.jsx` - дашборд безопасности

---

## 🚀 Порядок реализации

### Этап 1: Базовая авторизация (1-2 дня)
1. ✅ Настроить Supabase Auth
2. ✅ Создать таблицу profiles
3. ✅ Компоненты Login, Register
4. ✅ AuthContext для управления состоянием

### Этап 2: Управление пользователями (1 день)
1. ✅ Компонент UserManagement
2. ✅ CRUD операции с пользователями
3. ✅ Назначение ролей
4. ✅ Блокировка пользователей

### Этап 3: Блокировка IP (1 день)
1. ✅ Таблицы ip_blacklist, ip_whitelist
2. ✅ Компонент IPBlacklist
3. ✅ Middleware для проверки IP
4. ✅ Автоматическая блокировка

### Этап 4: Защита от спама (1-2 дня)
1. ✅ Rate limiting
2. ✅ reCAPTCHA
3. ✅ Фильтры спама
4. ✅ Модерация комментариев

### Этап 5: Мониторинг (1 день)
1. ✅ Таблица security_alerts
2. ✅ Компонент SecurityAlerts
3. ✅ Автоматические уведомления
4. ✅ Email уведомления

### Этап 6: 2FA (опционально, 1-2 дня)
1. ✅ Таблицы two_factor_auth
2. ✅ Email коды
3. ✅ TOTP (Google Authenticator)
4. ✅ Резервные коды

### Этап 7: Аналитика (1-2 дня)
1. ✅ SecurityDashboard
2. ✅ Графики и метрики
3. ✅ Интерактивная карта
4. ✅ Экспорт данных

---

## 💰 Стоимость и ресурсы

### Бесплатные решения:
- ✅ Supabase Auth (бесплатно до 50,000 пользователей)
- ✅ Google reCAPTCHA (бесплатно)
- ✅ Email через Resend (бесплатно до 3,000 писем/месяц)
- ✅ Геолокация через BigDataCloud (бесплатно)

### Платные (опционально):
- 💰 SMS 2FA (~$0.01 за SMS)
- 💰 Расширенная аналитика (Google Analytics Pro)
- 💰 DDoS защита (Cloudflare Pro - $20/месяц)

---

## 🎯 Итого

**Полная система безопасности включает:**
- 👥 Регистрация и авторизация пользователей
- 🔐 Управление пользователями и ролями
- 🚫 Блокировка по IP адресам
- ⚡ Защита от спама и DDoS
- ⚠️ Мониторинг подозрительной активности
- 📧 Email уведомления
- 🔒 Двухфакторная аутентификация (опционально)
- 📊 Аналитика и дашборд безопасности

**Время реализации:** 7-10 дней работы

**Сложность:** Средняя (требуется знание React, Supabase, SQL)

---

## 📝 Примечания

- Все функции можно реализовать постепенно
- Начать лучше с базовой авторизации
- Затем добавлять функции по мере необходимости
- Supabase уже имеет встроенную систему авторизации (упрощает работу)
- Большинство функций можно реализовать бесплатно

Хочешь начать с какого-то конкретного этапа? Скажи и я начну реализацию! 🚀
