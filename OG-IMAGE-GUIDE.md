# 🖼️ Руководство по OG изображениям (Open Graph Images)

## Что это такое?

Open Graph изображения - это превью-картинки, которые показываются когда вы делитесь ссылкой на пост в социальных сетях (Telegram, Twitter, Facebook, WhatsApp и т.д.).

## Как это работает?

### 1. Автоматическая генерация

Когда вы создаете или редактируете пост, система автоматически генерирует красивое превью с:
- Названием поста
- Категорией (Blog, News, Tutorial, Project)
- Градиентным фоном
- Логотипом блога

### 2. Использование в админ-панели

В админ-панели при редактировании поста вы увидите компонент **OG Image Preview** который позволяет:

- **Просмотреть** - как будет выглядеть превью
- **Скачать** - сохранить изображение на компьютер
- **Загрузить в Supabase** - автоматически загрузить в хранилище и привязать к посту
- **Копировать URL** - скопировать ссылку на изображение

### 3. Настройка Supabase Storage

Перед использованием убедитесь что у вас настроен bucket `blog-images` в Supabase Storage:

1. Откройте Supabase Dashboard
2. Перейдите в Storage
3. Создайте bucket с именем `blog-images` (если его нет)
4. Установите публичный доступ для чтения

### 4. Применение в коде

Компонент `OGImagePreview` можно добавить в любую форму редактирования поста:

\`\`\`jsx
import OGImagePreview from '../components/OGImagePreview'

// В форме редактирования
<OGImagePreview 
  title={postTitle} 
  category={post.category}
  postId={post.id}
/>
\`\`\`

## Технические детали

### Размеры изображения
- Ширина: 1200px
- Высота: 630px
- Формат: PNG
- Соотношение сторон: 1.91:1 (стандарт Open Graph)

### Поддерживаемые платформы
- ✅ Telegram
- ✅ Twitter/X
- ✅ Facebook
- ✅ LinkedIn
- ✅ WhatsApp
- ✅ Discord
- ✅ Slack

### SEO теги

Компонент `SEOHead` автоматически добавляет необходимые мета-теги:

\`\`\`html
<meta property="og:image" content="URL_изображения" />
<meta property="og:image:secure_url" content="URL_изображения" />
<meta property="og:image:width" content="1200" />
<meta property="og:image:height" content="630" />
<meta property="og:image:type" content="image/png" />
<meta property="og:image:alt" content="Название_поста" />
<meta name="twitter:card" content="summary_large_image" />
<meta name="twitter:image" content="URL_изображения" />
\`\`\`

## Примеры использования

### Пример 1: Создание нового поста

1. Напишите пост в админ-панели
2. Система автоматически сгенерирует превью
3. Нажмите "Загрузить в Supabase"
4. Опубликуйте пост
5. Поделитесь ссылкой в Telegram - увидите красивое превью!

### Пример 2: Обновление существующего поста

1. Откройте пост на редактирование
2. Измените название или категорию
3. Превью обновится автоматически
4. Загрузите новое изображение в Supabase

### Пример 3: Кастомное изображение

Если автоматическая генерация вам не подходит:

1. Создайте свое изображение (1200x630px)
2. Загрузите его в Supabase Storage вручную
3. Скопируйте публичный URL
4. Добавьте URL в поле `og_image` в базе данных

## Устранение проблем

### Превью не показывается в Telegram

1. Проверьте что изображение загружено в Supabase
2. Убедитесь что bucket `blog-images` публичный
3. Проверьте URL изображения в поле `og_image`
4. Telegram кэширует превью - попробуйте через несколько минут

### Изображение не генерируется

1. Проверьте консоль браузера на ошибки
2. Убедитесь что Canvas API поддерживается браузером
3. Проверьте что название поста не пустое

### Ошибка загрузки в Supabase

1. Проверьте настройки Storage в Supabase
2. Убедитесь что bucket существует
3. Проверьте права доступа (RLS policies)

## Дополнительные возможности

### Кастомизация дизайна

Отредактируйте файл `src/utils/ogImageGenerator.js` чтобы изменить:
- Цвета градиента
- Шрифты
- Размещение элементов
- Добавить логотип

### Пример кастомизации:

\`\`\`javascript
// Изменить градиент
const gradient = ctx.createLinearGradient(0, 0, 1200, 630)
gradient.addColorStop(0, '#FF6B6B')  // Красный
gradient.addColorStop(1, '#4ECDC4')  // Бирюзовый
\`\`\`

## Полезные ссылки

- [Open Graph Protocol](https://ogp.me/)
- [Twitter Card Validator](https://cards-dev.twitter.com/validator)
- [Facebook Sharing Debugger](https://developers.facebook.com/tools/debug/)
- [Telegram Bot API](https://core.telegram.org/bots/api)

## Поддержка

Если у вас возникли вопросы или проблемы, проверьте:
1. Консоль браузера на ошибки
2. Логи Supabase
3. Настройки Storage bucket
4. Правильность URL изображений
