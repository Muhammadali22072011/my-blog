# 🚀 Деплой на Vercel - Инструкция

## ✅ Подготовка завершена

Проект оптимизирован для продакшена с:
- Минификацией и удалением console.log
- Code splitting для быстрой загрузки
- Кэшированием статических ассетов
- Безопасными HTTP заголовками
- Оптимизацией зависимостей

## 📋 Шаги для деплоя

### 1. Установите Vercel CLI (опционально)
```bash
npm install -g vercel
```

### 2. Деплой через Vercel Dashboard (рекомендуется)

1. Зайдите на [vercel.com](https://vercel.com)
2. Нажмите **"Add New Project"**
3. Импортируйте ваш Git репозиторий
4. Vercel автоматически определит Vite проект

### 3. Настройте переменные окружения

В настройках проекта на Vercel добавьте:

```
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
VITE_ADMIN_EMAIL=admin@yourdomain.com
```

⚠️ **Важно**: Все переменные должны начинаться с `VITE_`

### 4. Настройки билда (автоматически)

Vercel использует настройки из `vercel.json`:
- Build Command: `npm run build`
- Output Directory: `dist`
- Install Command: `npm install`

### 5. Деплой через CLI (альтернатива)

```bash
cd my-blog-main
vercel
```

Следуйте инструкциям в терминале.

## 🔧 Оптимизации в продакшене

### Vite конфигурация включает:
- ✅ Минификация с Terser
- ✅ Удаление console.log
- ✅ Code splitting (React, Supabase, UI отдельно)
- ✅ Оптимизация размера чанков
- ✅ Отключены source maps (меньше размер)

### Vercel конфигурация включает:
- ✅ Кэширование ассетов (1 год для /assets/*)
- ✅ Безопасные HTTP заголовки
- ✅ Кэширование sitemap и robots.txt
- ✅ SPA routing (все роуты → index.html)

## 🧪 Тестирование перед деплоем

```bash
# Соберите продакшен билд
npm run build

# Протестируйте локально
npm run preview
```

Откройте http://localhost:4173 и проверьте работу.

## 📊 Проверка после деплоя

1. **Производительность**: [PageSpeed Insights](https://pagespeed.web.dev/)
2. **SEO**: Проверьте meta теги и Open Graph
3. **Безопасность**: [Security Headers](https://securityheaders.com/)
4. **Функциональность**: Проверьте все страницы и функции

## 🔄 Автоматический деплой

После первого деплоя, каждый push в main/master ветку автоматически деплоится на Vercel.

## 🌐 Кастомный домен

1. В настройках проекта на Vercel → **Domains**
2. Добавьте ваш домен
3. Настройте DNS записи (Vercel покажет инструкции)

## ⚡ Команды

```bash
# Разработка
npm run dev

# Продакшен билд
npm run build

# Превью продакшена локально
npm run preview

# Линтинг
npm run lint
```

## 🐛 Troubleshooting

### Ошибка "Module not found"
```bash
npm install
npm run build
```

### Переменные окружения не работают
- Убедитесь что они начинаются с `VITE_`
- Пересоберите проект после изменения .env

### 404 на роутах
- Проверьте что `vercel.json` содержит rewrites
- Убедитесь что используется React Router правильно

## 📝 Checklist перед деплоем

- [ ] Все переменные окружения настроены в Vercel
- [ ] Supabase RLS политики настроены
- [ ] Тестовый билд работает локально (`npm run preview`)
- [ ] .gitignore содержит .env файлы
- [ ] Удалены тестовые данные и console.log
- [ ] Проверены все роуты и функции
- [ ] Настроен кастомный домен (опционально)

---

✨ **Готово!** Ваш блог оптимизирован и готов к деплою на Vercel.
