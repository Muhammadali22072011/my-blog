# 🔧 Исправление превью в Telegram

## Проблема
Когда вы делитесь ссылкой на пост в Telegram, не отображается изображение в превью.

## Причины
1. У поста нет `featured_image` или `og_image` в базе данных
2. URL изображения неправильно сформирован
3. Telegram кэширует старые превью

## ✅ Решение

### Шаг 1: Проверьте изображения в постах

Выполните в Supabase SQL Editor:
```sql
SELECT 
  id,
  SUBSTRING(content, 1, 50) as title_preview,
  featured_image,
  og_image
FROM posts
WHERE status = 'published'
ORDER BY created_at DESC;
```

### Шаг 2: Добавьте изображение к посту

#### Вариант A: Через админ-панель
1. Откройте админ-панель вашего блога
2. Найдите пост (например, ID 98 про кибербезопасность)
3. Загрузите изображение в поле "Featured Image"
4. Сохраните

#### Вариант B: Через SQL
```sql
-- Установите изображение для конкретного поста
UPDATE posts
SET featured_image = 'images/blog-images/cybersecurity.png'
WHERE id = 98;
```

### Шаг 3: Очистите кэш Telegram

После добавления изображения, Telegram нужно обновить кэш:

1. **Используйте Telegram Bot API:**
   ```
   https://api.telegram.org/bot<YOUR_BOT_TOKEN>/sendMessage?chat_id=@username&text=https://izzatullaev.uz/post/98
   ```

2. **Или используйте специальный сервис:**
   - Откройте: https://developers.facebook.com/tools/debug/
   - Вставьте URL вашего поста
   - Нажмите "Scrape Again"
   - Это обновит кэш для всех соцсетей, включая Telegram

3. **Или просто подождите:**
   - Telegram обновит кэш через 24 часа

### Шаг 4: Проверьте результат

1. Откройте консоль браузера (F12)
2. Перейдите на страницу поста
3. Проверьте логи:
   ```
   🖼️ Post OG Image: { featured_image: "...", og_image: "...", final: "..." }
   🖼️ OG Image set for Telegram: https://...
   ```

4. Проверьте HTML код страницы (Ctrl+U):
   ```html
   <meta property="og:image" content="https://rfppkhwqnlkpjemmoexg.supabase.co/storage/v1/object/public/images/blog-images/your-image.png">
   ```

## 📝 Рекомендации

### Размеры изображений для Telegram
- **Минимум:** 200x200 px
- **Рекомендуется:** 1200x630 px (стандарт OG)
- **Формат:** JPG, PNG
- **Размер файла:** до 5 MB

### Где хранить изображения
В Supabase Storage:
1. Bucket: `images`
2. Папка: `blog-images`
3. Путь в БД: `images/blog-images/filename.png`

### Автоматическое извлечение изображений
Если у поста нет `featured_image`, система автоматически:
1. Проверит `og_image`
2. Найдет первое изображение в контенте поста
3. Использует его для превью

## 🐛 Отладка

### Проблема: Изображение не загружается
```sql
-- Проверьте права доступа к storage
SELECT * FROM storage.buckets WHERE name = 'images';

-- Проверьте политики
SELECT * FROM storage.policies WHERE bucket_id = 'images';
```

### Проблема: URL неправильный
Правильный формат:
```
https://rfppkhwqnlkpjemmoexg.supabase.co/storage/v1/object/public/images/blog-images/image.png
```

Неправильный:
```
/images/blog-images/image.png  ❌
images/blog-images/image.png   ❌
```

## ✨ Что исправлено

1. ✅ Улучшена функция `getFullImageUrl()` - правильно формирует URL
2. ✅ Добавлены console.log для отладки
3. ✅ Исправлен порядок OG тегов для Telegram
4. ✅ Добавлен `og:image:type` = `image/jpeg`
5. ✅ Автоматическое извлечение изображений из контента

## 🚀 Быстрый тест

1. Откройте пост в браузере
2. Нажмите F12 → Console
3. Найдите: `🖼️ OG Image set for Telegram: https://...`
4. Скопируйте URL и откройте в новой вкладке
5. Если изображение загружается → всё ОК! ✅

Теперь поделитесь ссылкой в Telegram и проверьте превью!
