# 🛡️ Руководство по кибербезопасности

## Что добавлено?

### 1. Security Dashboard ✅
Панель мониторинга безопасности в админ-панели с:
- Статистикой событий безопасности
- Логами всех действий
- Настройками защиты
- Визуальными индикаторами угроз

### 2. Security Logger ✅
Система логирования событий безопасности:
- Автоматическое логирование всех действий
- Определение уровня серьезности
- Отслеживание IP адресов
- Запись User Agent

### 3. Защита от атак ✅
- Защита от SQL Injection
- Защита от XSS атак
- Защита от брутфорса
- Защита от Path Traversal
- Защита от Command Injection

### 4. База данных ✅
Таблица `security_logs` для хранения:
- Типа события
- Уровня серьезности (low, medium, high, critical)
- Действия системы (allowed, blocked, warning)
- IP адреса и User Agent
- Дополнительных деталей

## Как использовать?

### Шаг 1: Создайте таблицу логов

Выполните SQL скрипт в Supabase:

\`\`\`bash
# Откройте Supabase SQL Editor
# Выполните create-security-logs.sql
\`\`\`

### Шаг 2: Откройте Security Dashboard

1. Откройте админ-панель
2. Перейдите на таб "🛡️ Безопасность"
3. Увидите:
   - Статистику событий
   - Последние логи
   - Настройки защиты

### Шаг 3: Интегрируйте логирование

В вашем коде используйте:

\`\`\`javascript
import { logSecurityEvent, SecurityEventType, SecuritySeverity } from '../utils/securityLogger'

// Логирование успешного входа
await logSecurityEvent({
  eventType: SecurityEventType.SUCCESSFUL_LOGIN,
  severity: SecuritySeverity.LOW,
  action: SecurityAction.ALLOWED,
  details: { username: 'admin' }
})

// Логирование подозрительной активности
await logSecurityEvent({
  eventType: SecurityEventType.SUSPICIOUS_ACTIVITY,
  severity: SecuritySeverity.HIGH,
  action: SecurityAction.BLOCKED,
  details: { reason: 'SQL injection attempt' }
})
\`\`\`

## Типы событий

### Аутентификация
- `LOGIN_ATTEMPT` - попытка входа
- `FAILED_LOGIN` - неудачный вход
- `SUCCESSFUL_LOGIN` - успешный вход
- `LOGOUT` - выход
- `PASSWORD_CHANGE` - смена пароля

### Атаки
- `BRUTE_FORCE_ATTEMPT` - попытка брутфорса
- `SQL_INJECTION_ATTEMPT` - попытка SQL инъекции
- `XSS_ATTEMPT` - попытка XSS атаки
- `UNAUTHORIZED_ACCESS` - несанкционированный доступ

### Действия
- `DATA_EXPORT` - экспорт данных
- `SETTINGS_CHANGE` - изменение настроек
- `USER_CREATED` - создание пользователя
- `USER_DELETED` - удаление пользователя
- `POST_CREATED` - создание поста
- `POST_DELETED` - удаление поста

## Уровни серьезности

### 🔵 LOW (Низкий)
Обычные действия, не представляющие угрозы:
- Успешный вход
- Создание поста
- Просмотр страниц

### 🟡 MEDIUM (Средний)
Действия требующие внимания:
- Неудачная попытка входа
- Изменение настроек
- Экспорт данных

### 🟠 HIGH (Высокий)
Подозрительная активность:
- Множественные неудачные попытки входа
- Попытки SQL инъекции
- Попытки XSS атак

### 🔴 CRITICAL (Критический)
Серьезные угрозы:
- Брутфорс атаки
- Успешный взлом
- Несанкционированный доступ к данным

## Защита от атак

### SQL Injection

Система автоматически определяет попытки SQL инъекции:

\`\`\`javascript
import { detectSuspiciousActivity } from '../utils/securityLogger'

const userInput = "'; DROP TABLE users; --"
if (detectSuspiciousActivity(userInput)) {
  // Блокируем запрос
  await logSecurityEvent({
    eventType: SecurityEventType.SQL_INJECTION_ATTEMPT,
    severity: SecuritySeverity.CRITICAL,
    action: SecurityAction.BLOCKED
  })
}
\`\`\`

### XSS (Cross-Site Scripting)

Санитизация пользовательского ввода:

\`\`\`javascript
import { sanitizeInput } from '../utils/securityLogger'

const userInput = '<script>alert("XSS")</script>'
const safe = sanitizeInput(userInput)
// Результат: &lt;script&gt;alert(&quot;XSS&quot;)&lt;/script&gt;
\`\`\`

### Брутфорс

Защита от множественных попыток входа:

\`\`\`javascript
import { checkBruteForce } from '../utils/securityLogger'

const email = 'user@example.com'
if (!checkBruteForce(email)) {
  // Блокируем после 5 попыток за 15 минут
  return { error: 'Too many attempts. Try again later.' }
}
\`\`\`

### Проверка силы пароля

\`\`\`javascript
import { checkPasswordStrength } from '../utils/securityLogger'

const password = 'MyP@ssw0rd123'
const strength = checkPasswordStrength(password)

console.log(strength)
// {
//   score: 6,
//   level: 'strong',
//   feedback: []
// }
\`\`\`

## Настройки безопасности

### Защита от брутфорса
- Блокировка после 5 неудачных попыток
- Время блокировки: 15 минут
- Автоматическое логирование

### Двухфакторная аутентификация (2FA)
- Дополнительная защита входа
- Коды через email/SMS/приложение
- Обязательна для админов

### Логирование
- Запись всех событий
- Автоматическая очистка старых логов (90 дней)
- Уведомления о критических событиях

## Мониторинг

### Что отслеживать?

**Ежедневно:**
- Критические события
- Заблокированные IP
- Подозрительная активность

**Еженедельно:**
- Статистика попыток входа
- Анализ логов
- Проверка настроек

**Ежемесячно:**
- Аудит безопасности
- Обновление правил
- Обучение пользователей

### Метрики

- **Всего событий** - общее количество записей
- **Заблокировано IP** - уникальные заблокированные адреса
- **Подозрительная активность** - события высокого уровня
- **Последняя атака** - дата последнего инцидента

## Уведомления

### Критические события

При критических событиях система автоматически:
1. Записывает в лог
2. Выводит в консоль
3. Может отправить уведомление (настраивается)

### Настройка уведомлений

Отредактируйте `src/utils/securityLogger.js`:

\`\`\`javascript
const sendSecurityAlert = async (logEntry) => {
  // Telegram
  await fetch(\`https://api.telegram.org/bot\${BOT_TOKEN}/sendMessage\`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      chat_id: CHAT_ID,
      text: \`🚨 CRITICAL: \${logEntry.event_type}\`
    })
  })
  
  // Email
  await sendEmail({
    to: 'admin@example.com',
    subject: 'Security Alert',
    body: \`Critical event: \${logEntry.event_type}\`
  })
}
\`\`\`

## Лучшие практики

### Для администраторов

1. **Сильные пароли**
   - Минимум 12 символов
   - Заглавные и строчные буквы
   - Цифры и спецсимволы
   - Не используйте распространенные пароли

2. **Двухфакторная аутентификация**
   - Включите 2FA для всех админов
   - Используйте приложения-аутентификаторы
   - Храните резервные коды

3. **Регулярные проверки**
   - Проверяйте логи ежедневно
   - Анализируйте подозрительную активность
   - Обновляйте систему регулярно

4. **Ограничение доступа**
   - Минимальные необходимые права
   - Регулярный аудит пользователей
   - Удаление неактивных аккаунтов

### Для разработчиков

1. **Валидация ввода**
   - Проверяйте все пользовательские данные
   - Используйте whitelist, а не blacklist
   - Санитизируйте перед сохранением

2. **Параметризованные запросы**
   - Используйте prepared statements
   - Никогда не конкатенируйте SQL
   - Используйте ORM (Supabase)

3. **Безопасное хранение**
   - Хешируйте пароли (bcrypt, argon2)
   - Шифруйте чувствительные данные
   - Используйте HTTPS

4. **Логирование**
   - Логируйте все важные действия
   - Не логируйте пароли и токены
   - Регулярно анализируйте логи

## Реагирование на инциденты

### При обнаружении атаки:

1. **Немедленно**
   - Заблокируйте IP атакующего
   - Проверьте логи на другие попытки
   - Оцените масштаб

2. **В течение часа**
   - Уведомите команду
   - Измените пароли если нужно
   - Проверьте целостность данных

3. **В течение дня**
   - Проведите полный аудит
   - Обновите правила безопасности
   - Документируйте инцидент

4. **После инцидента**
   - Анализ причин
   - Улучшение защиты
   - Обучение команды

## Compliance (Соответствие)

### GDPR
- Логирование доступа к персональным данным
- Право на удаление данных
- Уведомление о нарушениях (72 часа)

### Хранение логов
- Минимум: 30 дней
- Рекомендуется: 90 дней
- Максимум: 1 год (по закону)

## Инструменты

### Анализ безопасности
- [OWASP ZAP](https://www.zaproxy.org/) - сканер уязвимостей
- [Burp Suite](https://portswigger.net/burp) - тестирование безопасности
- [Nmap](https://nmap.org/) - сканирование портов

### Мониторинг
- [Sentry](https://sentry.io/) - отслеживание ошибок
- [LogRocket](https://logrocket.com/) - мониторинг фронтенда
- [Datadog](https://www.datadoghq.com/) - мониторинг инфраструктуры

## Чеклист безопасности

### Базовая безопасность
- [ ] Сильные пароли для всех аккаунтов
- [ ] Двухфакторная аутентификация
- [ ] HTTPS для всех соединений
- [ ] Регулярные обновления системы

### Защита от атак
- [ ] Валидация всех входных данных
- [ ] Санитизация пользовательского ввода
- [ ] Параметризованные SQL запросы
- [ ] Rate limiting для API

### Мониторинг
- [ ] Логирование всех действий
- [ ] Ежедневная проверка логов
- [ ] Уведомления о критических событиях
- [ ] Регулярный аудит безопасности

### Резервное копирование
- [ ] Ежедневные бэкапы базы данных
- [ ] Хранение бэкапов в безопасном месте
- [ ] Тестирование восстановления
- [ ] План аварийного восстановления

## Заключение

Безопасность - это непрерывный процесс, а не одноразовая задача. Регулярно:
- Проверяйте логи
- Обновляйте систему
- Обучайте команду
- Улучшайте защиту

**Помните:** Лучше предотвратить атаку, чем устранять последствия! 🛡️
