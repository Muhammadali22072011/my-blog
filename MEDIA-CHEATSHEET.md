# 📹 Шпаргалка по медиа

## 🚀 Быстрый старт

### 1. Настройка Storage (один раз)

```sql
-- Выполните в Supabase SQL Editor
-- Откройте файл: fix-storage-policies.sql
-- Нажмите RUN
```

### 2. Создайте buckets в Supabase Dashboard

- Storage → New bucket → `images` (Public: ON)
- Storage → New bucket → `videos` (Public: ON)
- Storage → New bucket → `avatars` (Public: ON)

### 3. Готово! ✅

---

## 📤 Загрузка

### Через код:
```javascript
import supabaseService from '../services/SupabaseService'

// Изображение
const img = await supabaseService.uploadImage(file)
console.log(img.url)

// Видео
const vid = await supabaseService.uploadVideo(file)
console.log(vid.url)
```

### Через компонент:
```jsx
<MediaUploader 
  type="video"
  onMediaUploaded={(result) => console.log(result.url)}
/>
```

---

## 📝 Вставка в посты

### Изображение:
```markdown
![Описание](URL)
```

### Видео (кастомный плеер):
```markdown
[🎥 Video: Название](URL)
```

### Видео (HTML):
```html
<video src="URL" title="Название"></video>
```

---

## 🎨 Компоненты

```jsx
// Кастомный плеер
<CustomVideoPlayer src="URL" title="Название" />

// Загрузчик
<MediaUploader type="video" onMediaUploaded={callback} />

// Галерея
<MediaGallery onSelectMedia={callback} />
```

---

## 📊 Лимиты

- Изображения: **5 MB**
- Видео: **50 MB**
- Аватарки: **2 MB**

---

## 🎯 Форматы

**Изображения:** JPG, PNG, GIF, WebP, SVG  
**Видео:** MP4, WebM, AVI, MOV, MKV, M4V

---

## 🔧 Решение проблем

### Не загружается?
1. Проверьте bucket создан и публичный
2. Выполните `fix-storage-policies.sql`
3. Проверьте размер файла

### Не воспроизводится?
1. Используйте MP4 формат
2. Проверьте URL (должен быть публичным)
3. Откройте консоль браузера

---

## ✨ Фичи плеера

- ▶️ Play/Pause
- 🔊 Громкость + Mute
- ⏱️ Перемотка
- 🖥️ Полный экран
- 🎨 Адаптивный дизайн
- 🎯 Автоскрытие контролов

---

## 📚 Файлы

- `CustomVideoPlayer.jsx` - Кастомный плеер
- `MediaUploader.jsx` - Загрузчик файлов
- `MediaGallery.jsx` - Галерея медиа
- `SupabaseService.js` - API методы
- `markdownRenderer.jsx` - Рендеринг видео
- `fix-storage-policies.sql` - Настройка прав

---

## 🎉 Готово!

Все работает! Загружайте медиа и используйте кастомный плеер! 🚀
