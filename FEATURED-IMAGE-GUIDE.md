# 🖼️ Главное изображение поста (Featured Image)

## Что это?

**Featured Image** - это главное изображение поста, которое показывается:
- ✅ В списке блогов (как миниатюра)
- ✅ В самом посте (как главное изображение)
- ✅ В Telegram/соцсетях (когда делишься ссылкой)
- ✅ Везде где показывается превью поста

## Установка

### 1. Добавь поле в базу данных

Выполни SQL в Supabase:

```sql
ALTER TABLE posts 
ADD COLUMN IF NOT EXISTS featured_image TEXT;
```

Или используй файл: `add-featured-image.sql`

### 2. Как использовать

#### В админ панели:

1. Создай/редактируй пост
2. Загрузи изображение в медиа галерею
3. Скопируй URL изображения
4. Вставь в поле `featured_image` в базе данных

#### Автоматически:

Если `featured_image` не задано, система автоматически:
- Берет первое изображение из контента поста
- Использует его для превью

## Где показывается

### 1. Список блогов (/blogs)
```jsx
{post.featured_image && (
  <img src={post.featured_image} alt={post.title} />
)}
```

### 2. Сам пост (/post/ID)
```jsx
{post.featured_image && (
  <img src={post.featured_image} alt={post.title} />
)}
```

### 3. OG теги (Telegram/соцсети)
```jsx
<SEOHead
  image={post.featured_image || getFirstImage(post.content)}
/>
```

## Рекомендации

### Размер изображения:
- **Минимум**: 1200x630 пикселей
- **Оптимально**: 1200x630 или 1920x1080
- **Формат**: JPG, PNG, WebP

### Качество:
- Используй оптимизированные изображения
- Размер файла: до 500 KB
- Хорошее качество, но не слишком большой размер

## Приоритет изображений

Система использует изображения в таком порядке:

1. **featured_image** - если задано
2. **og_image** - если задано
3. **Первое изображение из контента** - автоматически

## Пример использования

```javascript
// В BlogPost.jsx
const postImage = post.featured_image || post.og_image || getFirstImage(post.content)

// Использование
<SEOHead
  title={post.title}
  description={post.excerpt}
  image={postImage}
/>
```

## 🎉 Готово!

Теперь каждый пост может иметь красивое главное изображение, которое показывается везде!
