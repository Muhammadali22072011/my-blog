# 🚀 Инструкция по миграции продакшен базы данных

## ⚠️ ВАЖНО: Перед началом

**ОБЯЗАТЕЛЬНО СДЕЛАЙТЕ БЭКАП БАЗЫ ДАННЫХ!**

### Как сделать бэкап в Supabase:

1. Откройте ваш проект в Supabase Dashboard
2. Перейдите в **Database** → **Backups**
3. Нажмите **Create backup** или используйте автоматический бэкап

---

## 📋 Что делает миграция

Этот скрипт **безопасно обновит** вашу продакшен базу:

✅ **Сохранит все существующие данные** (посты, комментарии, профиль и т.д.)  
✅ Добавит новые таблицы (закладки, реакции, рейтинги, проекты, безопасность)  
✅ Обновит структуру существующих таблиц  
✅ Конвертирует типы данных (JSONB → TEXT[], INTEGER → BIGINT)  
✅ Создаст все индексы и политики безопасности  
✅ Удалит устаревшие таблицы (lessons, users)

---

## 🔧 Как выполнить миграцию

### Шаг 1: Откройте Supabase SQL Editor

1. Зайдите в ваш проект Supabase
2. Перейдите в **Database** → **SQL Editor**
3. Нажмите **New query**

### Шаг 2: Скопируйте и выполните скрипт

1. Откройте файл `migrate-production-safe.sql`
2. Скопируйте **весь** содержимое файла
3. Вставьте в SQL Editor
4. Нажмите **Run** (или Ctrl+Enter)

### Шаг 3: Дождитесь завершения

Миграция может занять от 10 секунд до нескольких минут в зависимости от объема данных.

### Шаг 4: Проверьте результат

После выполнения проверьте:

```sql
-- Проверка что данные на месте
SELECT COUNT(*) FROM posts;
SELECT COUNT(*) FROM comments;
SELECT COUNT(*) FROM profile;

-- Проверка новых таблиц
SELECT COUNT(*) FROM bookmarks;
SELECT COUNT(*) FROM reactions;
SELECT COUNT(*) FROM projects;
```

---

## 🎯 Что изменится

### Обновленные таблицы:

- **posts** - добавлены поля: featured_image, views, likes, reading_time, scheduled_at
- **profile** - добавлены социальные ссылки, удалены email и bio
- **about_me** - конвертация skills из JSONB в TEXT[]
- **comments** - добавлены status, parent_id (для вложенных комментариев)

### Новые таблицы:

- **bookmarks** - закладки пользователей
- **reactions** - реакции на посты (лайки, огонь и т.д.)
- **post_ratings** - рейтинги постов (1-5 звезд)
- **newsletter_subscribers** - подписчики рассылки
- **projects** - портфолио проектов
- **post_views** - детальная аналитика просмотров
- **admin_secrets** - секреты для админки
- **admin_sessions** - сессии админа
- **admin_login_attempts** - попытки входа
- **security_logs** - логи безопасности

### Удаленные таблицы:

- **lessons** - больше не используется
- **users** - больше не используется

---

## 🔍 Проверка после миграции

### 1. Проверьте структуру таблиц

```sql
-- Посмотрите структуру posts
SELECT column_name, data_type 
FROM information_schema.columns 
WHERE table_name = 'posts';
```

### 2. Проверьте данные

```sql
-- Проверьте что посты на месте
SELECT id, title, status, created_at FROM posts LIMIT 5;

-- Проверьте комментарии
SELECT id, post_id, author_name, content FROM comments LIMIT 5;
```

### 3. Проверьте политики безопасности

```sql
-- Посмотрите политики для posts
SELECT * FROM pg_policies WHERE tablename = 'posts';
```

---

## 🆘 Если что-то пошло не так

### Ошибка при выполнении скрипта

1. **Прочитайте сообщение об ошибке** - оно подскажет что не так
2. **Проверьте что у вас есть права** на изменение схемы
3. **Попробуйте выполнить скрипт по частям** - скопируйте только нужный раздел

### Восстановление из бэкапа

Если нужно откатить изменения:

1. Перейдите в **Database** → **Backups**
2. Найдите бэкап сделанный перед миграцией
3. Нажмите **Restore**

---

## ✅ После успешной миграции

1. **Протестируйте приложение** - убедитесь что все работает
2. **Проверьте админку** - войдите и создайте тестовый пост
3. **Проверьте комментарии** - добавьте тестовый комментарий
4. **Настройте Storage** (если еще не настроено):
   - Создайте buckets: `images`, `avatars`, `videos`
   - Настройте публичный доступ для чтения

---

## 📞 Нужна помощь?

Если возникли проблемы:

1. Проверьте логи в Supabase Dashboard
2. Посмотрите документацию Supabase
3. Проверьте что все переменные окружения правильно настроены в `.env.production`

---

## 🎉 Готово!

После успешной миграции ваша база данных полностью обновлена и готова к работе с новыми функциями!
