# 🚀 Руководство по настройке блога

## ✅ Что уже сделано

1. ✅ Установлены все зависимости (React, Vite, Supabase, Tailwind CSS)
2. ✅ Настроена структура проекта
3. ✅ Созданы все компоненты и страницы
4. ✅ Реализована система комментариев
5. ✅ Добавлены реакции на посты
6. ✅ Создана система закладок
7. ✅ Настроена подписка на рассылку
8. ✅ Добавлена страница проектов с админ-панелью
9. ✅ Интегрированы toast-уведомления
10. ✅ Подготовлены SQL скрипты для базы данных

## 📋 Что нужно сделать

### 1. Настройка Supabase

#### Шаг 1: Создайте проект в Supabase
1. Зайдите на https://supabase.com
2. Создайте новый проект
3. Скопируйте URL и Anon Key

#### Шаг 2: Настройте переменные окружения
Создайте файл `.env` в корне проекта:
```env
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
```

#### Шаг 3: Выполните SQL скрипты
В Supabase Dashboard → SQL Editor выполните скрипты по порядку:

1. **supabase-schema.sql** - основные таблицы
2. **add-all-features.sql** - дополнительные функции
3. **fix-storage-policies.sql** - настройка хранилища
4. **secure-admin-rls.sql** - безопасность админ-панели

#### Шаг 4: Создайте Storage Buckets
В Supabase Dashboard → Storage создайте 3 bucket:

1. **images** (Public: ON)
   - Для изображений блога
   - Max file size: 5MB

2. **avatars** (Public: ON)
   - Для аватарок
   - Max file size: 2MB

3. **videos** (Public: ON)
   - Для видео
   - Max file size: 50MB

### 2. Настройка Email рассылки (опционально)

#### Вариант A: Через Resend API (рекомендуется)

1. Зарегистрируйтесь на https://resend.com (бесплатно до 3000 писем/месяц)
2. Получите API ключ
3. Откройте файл `supabase-email-webhook.sql`
4. Замените `YOUR_RESEND_API_KEY_HERE` на ваш ключ
5. Выполните скрипт в Supabase SQL Editor

#### Вариант B: Через Supabase Edge Function (более безопасно)

Следуйте инструкциям в файле `EMAIL-SETUP.md`

### 3. Настройка аутентификации

#### Установите пароль для админ-панели

В файле `src/components/ClockAuth.jsx` найдите строку:
```javascript
const CORRECT_PASSWORD = 'your-secure-password'
```

Замените `'your-secure-password'` на ваш пароль.

**Важно:** Для production используйте переменные окружения!

### 4. Запуск проекта

```bash
# Установка зависимостей (если еще не установлены)
npm install

# Запуск в режиме разработки
npm run dev

# Сборка для production
npm run build

# Предпросмотр production сборки
npm run preview
```

### 5. Первоначальная настройка через админ-панель

1. Откройте http://localhost:5173/admin
2. Введите пароль
3. Заполните профиль в разделе "Profile"
4. Настройте "About Me"
5. Создайте первый пост
6. Добавьте проекты в разделе "Projects"

## 🎨 Кастомизация

### Изменение цветовой схемы

Откройте `tailwind.config.js` и измените цвета:
```javascript
theme: {
  extend: {
    colors: {
      primary: '#your-color',
      secondary: '#your-color',
    }
  }
}
```

### Изменение шрифтов

В файле `index.css` измените:
```css
@import url('https://fonts.googleapis.com/css2?family=Your+Font&display=swap');

body {
  font-family: 'Your Font', sans-serif;
}
```

### Настройка мета-тегов

Откройте `index.html` и измените:
- `<title>` - название сайта
- `<meta name="description">` - описание
- `<meta property="og:*">` - Open Graph теги

## 📱 Функции блога

### Для читателей:
- 📖 Чтение постов с прогресс-баром
- 💬 Комментарии с вложенностью
- 👍 Реакции на посты
- 🔖 Закладки для сохранения постов
- 📧 Подписка на рассылку
- 🔍 Поиск по постам
- 🌓 Темная/светлая тема
- 📱 Адаптивный дизайн

### Для администратора:
- ✍️ Markdown редактор с предпросмотром
- 📸 Загрузка изображений и видео
- 📊 Аналитика постов
- 🗂️ Управление проектами
- ⏰ Отложенная публикация
- 💾 Автосохранение черновиков
- 📤 Экспорт/импорт данных

## 🔒 Безопасность

### Важные настройки для production:

1. **Измените пароль админ-панели**
2. **Настройте RLS политики в Supabase**
3. **Используйте переменные окружения для секретов**
4. **Включите HTTPS**
5. **Настройте CORS**

### Рекомендуемые RLS политики:

```sql
-- Только аутентифицированные пользователи могут создавать посты
CREATE POLICY "Authenticated users can create posts"
ON posts FOR INSERT
TO authenticated
WITH CHECK (true);

-- Только владелец может редактировать посты
CREATE POLICY "Users can update own posts"
ON posts FOR UPDATE
TO authenticated
USING (auth.uid() = user_id);
```

## 🐛 Решение проблем

### Ошибка: "Supabase 400 Bad Request"
- Проверьте правильность URL и Anon Key в `.env`
- Убедитесь, что выполнены все SQL скрипты
- Проверьте RLS политики в Supabase

### Ошибка: "Storage bucket not found"
- Создайте buckets в Supabase Storage
- Проверьте настройки публичного доступа
- Выполните `fix-storage-policies.sql`

### Ошибка: "Cannot read properties of null"
- Проверьте, что все таблицы созданы
- Убедитесь, что есть начальные данные
- Проверьте консоль браузера на ошибки

### Проблемы с email рассылкой
- Проверьте API ключ Resend
- Убедитесь, что триггер создан в БД
- Проверьте логи в Supabase Dashboard

## 📚 Дополнительные ресурсы

- [Документация Supabase](https://supabase.com/docs)
- [Документация React](https://react.dev)
- [Документация Tailwind CSS](https://tailwindcss.com/docs)
- [Markdown Guide](https://www.markdownguide.org)
- [Resend API Docs](https://resend.com/docs)

## 🆘 Поддержка

Если возникли проблемы:
1. Проверьте консоль браузера (F12)
2. Проверьте логи Supabase
3. Убедитесь, что все зависимости установлены
4. Проверьте версии Node.js (рекомендуется 18+)

## 🎉 Готово!

После выполнения всех шагов ваш блог готов к использованию!

Не забудьте:
- ⭐ Создать первый пост
- 📝 Заполнить профиль
- 🎨 Настроить дизайн под себя
- 📧 Протестировать email рассылку
- 🚀 Задеплоить на хостинг (Vercel, Netlify, etc.)

---

**Удачи с вашим блогом! 🚀**
